# Copy to .env and adjust values before running locally or in CI.
APP_MODULE=src.main:app
LOG_LEVEL=info
MODEL_CONFIG=configs/model.example.yaml
MODEL_DATA_DIR=/models
# Optional: token for accessing gated Hugging Face models
HF_TOKEN=
