# Base URL of the backend API; leave blank to use same-origin requests with Nginx/Compose proxy
VITE_API_BASE_URL=
# API prefix; defaults to /api
VITE_API_PREFIX=/api
# Optional dev port override
VITE_DEV_PORT=5173
